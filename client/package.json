{
  "scripts": {
    "build": "yarn transpile && yarn bundle && cp src/index.html dist/index.html && cp src/index.css dist/index.css",
    "bundle": "esbuild tsc.out/client/src/index.js --bundle --minify --outfile=dist/index.js --sourcemap --target=chrome58,firefox57,safari11,edge16",
    "transpile": "tsc"
  },
  "dependencies": {
    "phaser": "^3.55.2",
    "sockette": "^2.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.14.5",
    "typescript": "^4.5.4"
  }
}
