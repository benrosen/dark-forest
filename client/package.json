{
  "scripts": {
    "build": "yarn transpile && yarn bundle && yarn render",
    "bundle": "esbuild tsc.out/client/src/index.js --bundle --minify --outfile=dist/index.js",
    "render": "ts-node src/html.ts > dist/index.html && ts-node src/css.ts > dist/ndex.css",
    "transpile": "tsc"
  },
  "dependencies": {
    "phaser": "^3.55.2",
    "sockette": "^2.0.6",
    "tone": "^14.7.77"
  },
  "devDependencies": {
    "esbuild": "^0.14.5",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  },
  "data": {
    "colors": {
      "DARK": "#101010",
      "LIGHT": "#d6ecef"
    },
    "ids": {
      "CENTERED_CONTENT": "centered-content",
      "GAME_CONTAINER": "game-container",
      "POST_CLICK_CONTENT": "post-click-content",
      "PRE_CLICK_CONTENT": "pre-click-content",
      "VIEWPORT_CONTAINER": "viewport-container"
    }
  }
}
